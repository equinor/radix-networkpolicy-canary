apiVersion: radix.equinor.com/v1
kind: RadixApplication
metadata:
  name: radix-canary-golang
spec:
  environments:
    - name: dev
      build:
        from: master
    - name: prod
      build:
        from: release
  components:
    - name: www
      src: "."
      public: true
      monitoring: true
      ports:
        - name: http
          port: 5000

